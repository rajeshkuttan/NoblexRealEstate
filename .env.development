#VITE_API_URL=http://localhost:5002/api

# Optional: override host for /uploads/* if different from API origin (see getUploadsOrigin in src/lib/utils.ts)
# VITE_UPLOADS_ORIGIN=http://localhost:5002
# Client batch size for unit bulk import (each batch = one API call)
VITE_IMPORT_BATCH_SIZE=500
